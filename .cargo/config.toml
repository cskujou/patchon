[build]
target-dir = "rust/build"

[env]
# Allow building the PyO3 extension on newer CPython releases such as 3.14.
# This is primarily needed for Linux development until the pinned PyO3 version
# fully recognizes that interpreter version.
PYO3_USE_ABI3_FORWARD_COMPATIBILITY = "1"
